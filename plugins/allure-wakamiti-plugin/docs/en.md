An exporter that emits Wakamiti results in the format expected by Allure.

---
## Table of content

---
## Configuration

####  `allureReport.output`
The output directory where Allure result files are generated.

The default value is `allure-results`.

Example:

```yaml
allureReport:
  output: target/allure-results
```
