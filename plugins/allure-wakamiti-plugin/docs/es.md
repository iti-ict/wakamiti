Un exportador que genera resultados de Wakamiti en el formato esperado por Allure.

---
## Tabla de contenido

---
## Configuración

####  `allureReport.output`
El directorio de salida donde se generan los ficheros de resultados de Allure.

El valor por defecto es `allure-results`.

Ejemplo:

```yaml
allureReport:
  output: target/allure-results
```
