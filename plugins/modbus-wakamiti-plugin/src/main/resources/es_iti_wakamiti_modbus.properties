# Fill this file with the step translations in English, such as:
modbus.step1=Step 1
modbus.step2=Step 2
modbus.step3=Step 3

modbus.define.baseURL=the base URL {url}
modbus.define.slaveId=the slaveId {slaveId}

modbus.execute.read={quantity} (is|are) read from the address {address}

modbus.assert.read.value=registers read contains value {value}