name: Publish
on:
  push:
    branches:
    - feature/deploy-central-repository
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    #- name: Set up Maven Central Repository
    #  uses: actions/setup-java@v3
    #  with:
    #    java-version: '11'
        #distribution: 'adopt'
        #server-id: ossrh
        #server-username: sonatype_iti
        #server-password: bieth7Eiyaixep5+
    - name: Publish package
      run:  ./mvnw verify -pl kukumo-plugins/kukumo-rest  #mvn --batch-mode deploy
      #env:
      #  MAVEN_USERNAME: sonatype_iti
      #  MAVEN_PASSWORD: bieth7Eiyaixep5+
